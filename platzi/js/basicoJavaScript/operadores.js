//Aritmeticos, binarios por que interactúan entre dos elemtnos

2 + 2 //4
2 - 2 // 0
2 * 2 // 4
2 / 2  // 1

"Alan " + "Muñoz" // concatena los string
 
//Operadores unarios

!false //negacion, en este caso niega el falso por tanto devuelve verdadero true 

// asignación e igualdad

var ag = 0; //un solo gino igual representa asignación, de un valor a una variable

var a = 3;
var b = "3";
a == b  // retorna true, porque dos signos iguales comparan elementoz, centrandose solo en el valor.
a === b // devuelve false, ya que tres signos iguales tambien comparan pero en este caso tipo de dato y valor, en este caso un numero y un string no son iguales , aanque compartan valor
5 > 3; //Mayor que
5 < 3; //Menor que
5 >= 3;//Mayor o iguak a
5 <= 3;//Menor o igual a
a && b; //operador copulativo (and, Y)
a || b; //operador disyuntivo (or, O)
a ?? b


//operadores para incremetar
var edad = 40;
edad ++; // aunmetará la variable edad en 1, arroja 41 
edad += 2 // invremeta el valor en la catidad indicada, es igual a decir, edad = edad + 2
